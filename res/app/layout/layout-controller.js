module.exports =
  function LayoutCtrl(LanguageService, $rootScope, hotkeys, $filter, gettext) {
    LanguageService.updateLanguage()

    // Explicitly code removed to change admin settings
    // https://projects.cispa.saarland/timo.guehring/stf/issues/6
  }
